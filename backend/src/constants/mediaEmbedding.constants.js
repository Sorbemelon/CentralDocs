import { EMBEDDING_DIMENSIONS, EMBEDDING_MODEL, EMBEDDING_PROVIDER } from "./embedding.constants.js";

export const MEDIA_EMBEDDING_PROVIDER = EMBEDDING_PROVIDER;
export const MEDIA_EMBEDDING_MODEL = EMBEDDING_MODEL;
export const MEDIA_EMBEDDING_DIMENSIONS = EMBEDDING_DIMENSIONS;

export const CHUNK_KIND = Object.freeze({
  TEXT: "text",
  MEDIA_DIRECT: "media_direct",
});

export const CHUNK_KINDS = Object.freeze(Object.values(CHUNK_KIND));

export const EMBEDDING_INPUT_TYPE = Object.freeze({
  TEXT: "text",
  IMAGE: "image",
  AUDIO: "audio",
  VIDEO: "video",
});

export const EMBEDDING_INPUT_TYPES = Object.freeze(Object.values(EMBEDDING_INPUT_TYPE));

export const MEDIA_EMBEDDING_CHUNK_INDEX = 100000;

export const MEDIA_EMBEDDING_ERROR_CODE = Object.freeze({
  MEDIA_EMBEDDING_UNSUPPORTED: "MEDIA_EMBEDDING_UNSUPPORTED",
  MEDIA_EMBEDDING_MOCK_ONLY: "MEDIA_EMBEDDING_MOCK_ONLY",
  MEDIA_ASSET_NOT_FOUND: "MEDIA_ASSET_NOT_FOUND",
  MEDIA_ASSET_PATH_INVALID: "MEDIA_ASSET_PATH_INVALID",
  MEDIA_EMBEDDING_NOT_CONFIGURED: "MEDIA_EMBEDDING_NOT_CONFIGURED",
  MEDIA_EMBEDDING_PROVIDER_ERROR: "MEDIA_EMBEDDING_PROVIDER_ERROR",
  MEDIA_EMBEDDING_RATE_LIMITED: "MEDIA_EMBEDDING_RATE_LIMITED",
  AI_RATE_LIMIT_EXHAUSTED: "AI_RATE_LIMIT_EXHAUSTED",
  MEDIA_EMBEDDING_CACHE_FAILED: "MEDIA_EMBEDDING_CACHE_FAILED",
});
