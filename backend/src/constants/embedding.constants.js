import { AI_MODELS } from "../config/aiModels.js";

export const EMBEDDING_PROVIDER = "gemini";
export const EMBEDDING_MODEL = AI_MODELS.embedding.model;
export const EMBEDDING_DIMENSIONS = AI_MODELS.embedding.dimensions;

export const EMBEDDING_TASK_TYPE = Object.freeze({
  RETRIEVAL_DOCUMENT: "RETRIEVAL_DOCUMENT",
  RETRIEVAL_QUERY: "RETRIEVAL_QUERY",
});

export const EMBEDDING_LIMITS = Object.freeze({
  maxInputChars: 24000,
});

export const EMBEDDING_ERROR_CODE = Object.freeze({
  EMBEDDING_NOT_CONFIGURED: "EMBEDDING_NOT_CONFIGURED",
  EMBEDDING_INPUT_EMPTY: "EMBEDDING_INPUT_EMPTY",
  EMBEDDING_INPUT_TOO_LARGE: "EMBEDDING_INPUT_TOO_LARGE",
  EMBEDDING_PROVIDER_ERROR: "EMBEDDING_PROVIDER_ERROR",
  EMBEDDING_RATE_LIMITED: "EMBEDDING_RATE_LIMITED",
  AI_RATE_LIMIT_EXHAUSTED: "AI_RATE_LIMIT_EXHAUSTED",
  INVALID_EMBEDDING_VECTOR: "INVALID_EMBEDDING_VECTOR",
  INVALID_EMBEDDING_DIMENSIONS: "INVALID_EMBEDDING_DIMENSIONS",
  CHUNK_EMBEDDING_FAILED: "CHUNK_EMBEDDING_FAILED",
});
