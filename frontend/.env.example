# CentralDocs frontend environment template.
# Copy to .env.local for local development. Do NOT commit a real .env file.

# Base URL for the CentralDocs backend API (backend default PORT is 8080).
VITE_API_BASE_URL=http://localhost:8080/api

# Display name used across the shell.
VITE_APP_NAME=CentralDocs
